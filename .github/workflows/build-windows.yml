name: build-windows-x64

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'main.py'
      - '.github/workflows/build-windows.yml'
      - 'pyproject.toml'
      - 'uv.lock'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          architecture: 'x64'

      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller DrissionPage tablib

      - name: Build executable
        run: pyinstaller --noconfirm --clean --onefile --windowed --name xmz main.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: xmz-windows-x64
          path: dist/xmz.exe
          if-no-files-found: error
          retention-days: 14
